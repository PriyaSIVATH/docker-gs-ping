pipeline {
    agent {
        dockerfile true
    }

    stages {
        stage('checkout') {
            steps {
               // echo 'Hello World'
               checkout scm 
            }
        }
    }
}
